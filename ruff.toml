# tools/ の Python 用 lint 設定（実行は `mise run lint`）。
# 各設定の理由は docs/current/toolchain.md を参照。

target-version = "py312"
line-length = 100

[lint]
select = ["E", "W", "F", "I", "UP", "B", "SIM", "C4", "PIE", "FURB", "RUF"]
ignore = ["RUF001", "RUF002", "RUF003"]
